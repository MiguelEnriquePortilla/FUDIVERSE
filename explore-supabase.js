require('dotenv').config();  const { createClient } = require('@supabase/supabase-js');  const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_ANON_KEY);  async function exploreData() {    console.log('?? DISCOVERING RESTAURANT DATA SOURCES...');    const keyTables = ['restaurants', 'transactions', 'products', 'intelligent_product_daily', 'intelligent_payment_daily', 'intelligent_temporal_daily'];    for (const table of keyTables) {      console.log(`?? Checking table: ${table}...`);      try {        const { data, error } = await supabase.from(table).select('*').limit(2);        if (data && data.length > 0) {          console.log('? Found table:', table);          console.log('?? Sample structure:', Object.keys(data[0]));          console.log('?? Records count:', data.length);        } else {          console.log('? Table empty:', table);        }      } catch (err) {        console.log('? Table not accessible:', table);      }    }  }  exploreData().catch(console.error); 
